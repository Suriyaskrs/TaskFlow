package com.taskflow.model;

/**
 * Defines user roles in the system.
 * Currently supports USER and ADMIN.
 * Extensible for future role-based access control.
 */
public enum Role {
    USER,
    ADMIN
}